import React from 'react';
import { StyleSheet, View } from 'react-native';

export default Snowflake = () => <View style={styles.snowflake}></View>;
const styles = StyleSheet.create({
  snowflake: {
    backgroundColor: 'lightblue',
    marginBottom: 10,
    marginLeft: 13,
    marginRight: 13,
    height: 70,
    width: 90
  }
});